import 'package:flutter/material.dart';

/// Global navigator key — used by ApiClient to navigate to login
/// without requiring a BuildContext.
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
